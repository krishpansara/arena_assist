steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/arena-assist-unified', '-f', 'Dockerfile.cloud', '.']
options:
  machineType: 'E2_HIGHCPU_8'
images:
- 'gcr.io/$PROJECT_ID/arena-assist-unified'
